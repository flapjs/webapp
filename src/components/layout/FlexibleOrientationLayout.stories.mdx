import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FlexibleOrientationLayout from './FlexibleOrientationLayout.jsx';

<Meta title="@flapjs/components/layout/FlexibleOrientationLayout"
    component={FlexibleOrientationLayout}/>

# FlexibleOrientationLayout

A React component that takes a function as its child and orientate itself
on resize based on the passed-in media query.

<Props of={FlexibleOrientationLayout}/>
