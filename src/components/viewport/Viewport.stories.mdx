import { Meta, Props } from '@storybook/addon-docs/blocks';
import Viewport from './Viewport.jsx';

<Meta title="@flapjs/components/viewport/Viewport"
    component={Viewport}/>

# Viewport

A React component that needs some love. Please describe me! :(

<Props of={Viewport}/>

