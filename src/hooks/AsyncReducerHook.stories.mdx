import { Meta, Props } from '@storybook/addon-docs/blocks';
import { useAsyncReducer } from './AsyncReducerHook.jsx';

<Meta title="@flapjs/hooks/AsyncReducerHook"
    component={useAsyncReducer}/>

# AsyncReducerHook

A React hook that allows for asynchronous reducer functions.
This effectively replaces `useReducer()`.
