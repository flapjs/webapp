import PlaygroundLayer from './PlaygroundLayer.jsx';

const MODULE = {
    id: 'node',
    version: '1.0.0',
    serices: [],
    renders: {
        playground: [ PlaygroundLayer ]
    },
    load(session)
    {
    },
    unload(session)
    {
    }
};

export default MODULE;
